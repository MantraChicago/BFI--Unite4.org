.row
  .large-1.column
    img src="/assets/causes/ui/need_icons/followers.png"
  .large-11.column
    h2 Add new followers need
    a.close-reveal-modal &#215;
hr

=form_for @need do |f|
  =hidden_field_tag :cause_slug, @cause.slug
  =hidden_field_tag :need_type, @need_type
  .row
    .large-12.column
      label Need title
      =f.text_field :name
  .row
    .large-4.column
      label How many followers do you want
      =f.text_field :desired_state
  .row
    .large-12.column
      label Tell us what why you want followers?
      =f.text_area(:description, :rows => 5)
  .row
    .large-12.column
      = submit_tag 'Create Need', class: 'small button expand'